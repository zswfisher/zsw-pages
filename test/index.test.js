const zswPages = require('..')

// TODO: Implement module test
test('zsw-pages', () => {
  // expect(zswPages('w')).toBe('w@zce.me')
  // expect(zswPages('w', { host: 'wedn.net' })).toBe('w@wedn.net')
  // expect(() => zswPages(100)).toThrow('Expected a string, got number')
})
